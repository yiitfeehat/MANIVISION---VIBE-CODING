{
    "name": "vision-board-monorepo",
    "version": "1.0.0",
    "scripts": {
        "install": "cd client && npm install",
        "build": "cd client && npm run build"
    },
    "engines": {
        "node": "18.x"
    }
}